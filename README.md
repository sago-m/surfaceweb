SurfaceWeb — simple two-page blobs demo

What I changed
- Added a compact SurfaceWeb logo (assets/logo.svg).
- Updated index.html and blob.html to include the logo in the site header and added a favicon link to the SVG.
- Adjusted css/styles.css to style the header and logo.

How this looks
- The logo appears in the top-left of the header and is clickable back to the landing page.
- The SVG is also used as the site favicon (browsers that support SVG favicons will show it).

Preview locally
- Serve the repo locally: `python -m http.server`
- Open http://localhost:8000 and confirm the logo appears on both the landing and blob pages.

Next steps I can do for you
- Create alternate PNG / favicon.ico for broader browser support.
- Design a more elaborate logo (color variations, SVG refinement) or provide a downloadable brand package.
- Add a small animated entrance for the logo on page load.
